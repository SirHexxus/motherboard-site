'use strict';

// BASE_PATH: prefix for all fetch() calls to /data/*.json and asset paths.
// Custom domain (m0therb0ard.sirhexx.com) serves from root, so this stays ''.
// If deploying to GitHub Pages without a custom domain, set to '/repo-name'.
export const BASE_PATH = '';
